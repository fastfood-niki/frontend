---
import type { MenuCategory as MenuCategoryType } from '../../data/menu';
import SectionTitle from '../common/SectionTitle.astro';
import MenuItem from './MenuItem.astro';

interface Props {
	category: MenuCategoryType;
}

const { category } = Astro.props;
---

<section
	id={category.id}
	class="menu-category-anchor mb-6 break-inside-avoid last:mb-0"
	aria-labelledby={`${category.id}-heading`}
>
	<div
		data-category-section
		data-category-id={category.id}
		class="category-section-card group/card relative overflow-hidden rounded-3xl border border-red-900/45 bg-gradient-to-br from-zinc-900/90 via-zinc-950 to-black p-5 shadow-[0_24px_60px_-24px_rgba(0,0,0,0.72)] ring-1 ring-inset ring-red-500/[0.08] transition-[border-color,box-shadow] hover:border-red-700/55 hover:shadow-[0_28px_70px_-28px_rgba(255,59,32,0.18)] md:p-7"
	>
		<div
			class="pointer-events-none absolute -right-16 -top-16 h-40 w-40 rounded-full bg-red-500/[0.14] blur-3xl transition-opacity group-hover/card:opacity-90"
			aria-hidden="true"
		>
		</div>
		<SectionTitle title={category.title} categoryId={category.id} id={`${category.id}-heading`} />
		<ul class="relative divide-y divide-red-950/55">
			{
				category.items.map((item) => (
					<li>
						<MenuItem item={item} />
					</li>
				))
			}
		</ul>
	</div>
</section>
